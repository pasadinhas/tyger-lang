fn sum(a: i64, b: i64) -> i64 {
  return a + b;
}

fn double(n: i64) -> i64 {
  return 2 * n;
}

fn factorial(n: i64) -> i64 {
  if (n < 2) {
    return 1; 
  }
  else {
    return n * factorial(n - 1);
  }
}

sum(sum(double(factorial(5)), double(11)), 0-6);